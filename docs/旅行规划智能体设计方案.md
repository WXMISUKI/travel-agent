# 旅行规划智能体 - 完整设计方案

## 一、技术架构

### 1.1 技术栈（复用并优化anime-agent）

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **LLM** | MiniMax M2.5 | 通过DashScope调用 |
| **Agent框架** | LangChain + LangGraph | ReAct模式，流程编排 |
| **Web框架** | FastAPI | 后端API服务 |
| **前端** | Vue3 + Vite | 简单对话界面 |
| **数据源** | Open-Meteo天气API + 12306 MCP + 百度搜索 | 混合方案 |

### 1.2 项目结构（参考anime-agent优化）

```
travel-agent/
├── .env                          # 环境变量配置
├── .env.example                  # 环境变量示例
├── requirements.txt              # Python 依赖
├── README.md                     # 项目文档
├── src/
│   ├── __init__.py
│   ├── main.py                   # FastAPI 入口 + SSE流式输出
│   ├── config.py                 # 配置管理
│   ├── agent/
│   │   ├── __init__.py
│   │   ├── graph.py              # LangGraph 流程图
│   │   ├── state.py              # Agent 状态定义
│   │   └── tools.py              # LangChain Tools 定义
│   ├── skills/
│   │   ├── __init__.py
│   │   ├── base.py               # Skill 基类
│   │   ├── travel_plan.py        # 行程规划 Skill
│   │   ├── weather.py            # 天气查询 Skill
│   │   ├── ticket.py             # 车票查询 Skill
│   │   └── attraction.py         # 景点查询 Skill
│   ├── llm/
│   │   ├── __init__.py
│   │   ├── client.py             # MiniMax 客户端
│   │   └── prompts.py            # Prompt 模板
│   ├── data_sources/
│   │   ├── __init__.py
│   │   ├── weather.py            # Open-Meteo 天气API
│   │   ├── baidu_search.py       # 百度搜索降级
│   │   └── mcp_client.py         # 12306 MCP 客户端
│   ├── models/
│   │   ├── __init__.py
│   │   ├── travel.py             # 旅行数据模型
│   │   └── context.py            # 对话上下文
│   └── utils/
│       ├── __init__.py
│       ├── cache.py              # 缓存工具
│       └── logger.py             # 日志工具
└── frontend/                      # Web前端
    ├── index.html
    ├── src/
    │   ├── main.js
    │   ├── App.vue
    │   ├── components/
    │   │   └── ChatWindow.vue
    │   └── styles/
    │       └── main.css
    └── vite.config.js
```

---

## 二、工具(Tools)设计

### 2.1 工具列表

| 工具名称 | 功能 | 数据来源 |
|----------|------|----------|
| `get_weather` | 查询目的地天气 | Open-Meteo API |
| `get_train_tickets` | 查询火车票 | 12306 MCP |
| `get_train_route` | 查询列车经停站 | 12306 MCP |
| `search_attractions` | 搜索景点/美食 | 百度搜索 |
| `get_travel_tips` | 获取当地注意事项 | 百度搜索 |
| `get_current_date` | 获取当前日期 | 内置工具 |

### 2.2 工具定义（LangChain Format）

```python
# src/agent/tools.py

from langchain_core.tools import BaseTool
from pydantic import Field
from ..skills.weather import WeatherSkill
from ..skills.ticket import TicketSkill
from ..skills.attraction import AttractionSkill


class GetWeatherInput(BaseTool):
    """天气查询工具参数"""
    city: str = Field(description="城市名称，如'北京'、'上海'")


class GetWeatherTool(BaseTool):
    """天气查询工具"""
    name: str = "get_weather"
    description: str = """查询指定城市的天气预报
    
    返回当前天气和未来几天的预报，包括：
    - 当前温度、天气状况
    - 风速、湿度
    - 未来几天天气预报
    
    如果不知道城市名，先用百度搜索确认"""


class GetTrainTicketsInput(BaseTool):
    """火车票查询工具参数"""
    date: str = Field(description="出发日期，格式yyyy-MM-dd，如'2026-03-15'")
    from_station: str = Field(description="出发城市或车站名")
    to_station: str = Field(description="到达城市或车站名")
    train_type: str = Field(default="G", description="车次类型：G(高铁)/D(动车)/K(快速)/T(特快)/全部")


class GetTrainTicketsTool(BaseTool):
    """火车票查询工具"""
    name: str = "get_train_tickets"
    description: str = """查询12306火车票余票信息
    
    参数：
    - date: 出发日期（必须），格式yyyy-MM-dd
    - from_station: 出发城市或车站名
    - to_station: 到达城市或车站名
    - train_type: 车次类型筛选（G高铁/D动车/K快速/T特快，不填则查全部）
    
    返回：车次、出发/到达时间、历时、票价、余票等信息"""


class SearchAttractionsInput(BaseTool):
    """景点搜索工具参数"""
    city: str = Field(description="城市名称")
    keyword: str = Field(default="景点", description="搜索关键词，如'景点'、'美食'、'网红餐厅'")


class SearchAttractionsTool(BaseTool):
    """景点/美食搜索工具"""
    name: str = "search_attractions"
    description: str = """搜索城市内的景点、美食、网红打卡地
    
    当用户询问某个城市有什么好玩的好吃的时使用此工具
    
    参数：
    - city: 城市名称
    - keyword: 搜索类型（景点/美食/网红餐厅/必吃榜）"""


class GetTravelTipsInput(BaseTool):
    """当地注意事项工具参数"""
    city: str = Field(description="城市名称")


class GetTravelTipsTool(BaseTool):
    """当地注意事项工具"""
    name: str = "get_travel_tips"
    description: str = """获取目的地的旅行注意事项
    
    包括：
    - 天气穿衣建议
    - 当地交通建议
    - 必带物品建议
    - 注意事项和禁忌"""


def create_tools() -> list[BaseTool]:
    """创建所有LangChain Tools"""
    return [
        GetWeatherTool(),
        GetTrainTicketsTool(),
        SearchAttractionsTool(),
        GetTravelTipsTool(),
    ]
```

---

## 三、Agent流程设计（LangGraph）

### 3.1 ReAct模式流程

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户请求入口                               │
│              (CLI / API / WebSocket 流式响应)                    │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                     Agent 编排层 (ReAct 模式)                     │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                    ReAct Agent                              │ │
│  │  - 意图分析 → 工具选择 → 执行 → 评估 → 重试               │ │
│  │  - 支持多轮迭代，最大 5 次                                   │ │
│  │  - 自主决策重试策略                                          │ │
│  └────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                        LangChain Tools 层                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  │ get_weather  │  │get_train_    │  │search_at     │  │get_travel    │
│  │              │  │  tickets     │  │ tractions    │  │   _tips      │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ web_search Tool (百度搜索 - Fallback)                       │ │
│  └────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                        Skills 层                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  │   Weather   │  │   Ticket     │  │ Attraction  │  │ TravelTips  │
│  │   Skill     │  │   Skill      │  │   Skill     │  │   Skill     │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘
└────────────────────────────┬────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                    数据源适配器层                                │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐           │
│  │Open-Meteo    │  │  12306 MCP  │  │Baidu Search │           │
│  │  天气API     │  │   火车票    │  │   Fallback  │           │
│  └──────────────┘  └──────────────┘  └──────────────┘           │
│                                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ DataSource Router (数据源路由器 + 降级策略)                  │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 Agent状态定义

```python
# src/agent/state.py
from typing import TypedDict, Annotated
from langgraph.graph import add_messages

class AgentState(TypedDict):
    """Agent 状态定义"""
    # 输入
    messages: Annotated[list, add_messages]  # 消息历史
    user_query: str                          # 用户查询
    
    # 意图解析结果
    intent: dict | None                      # 解析后的意图
    query_params: dict | None                # 查询参数
    
    # 工具调用结果
    tool_results: dict | None                # 工具调用结果
    weather_data: dict | None                # 天气数据
    ticket_data: dict | None                # 车票数据
    attraction_data: dict | None            # 景点数据
    
    # 输出
    final_response: str | None               # 最终回复
    error: str | None                        # 错误信息
    
    # 元数据
    tools_used: list[str]                    # 使用的工具列表
    retry_count: int                        # 重试次数
```

### 3.3 LangGraph流程定义

```python
# src/agent/graph.py
from langgraph.graph import StateGraph, START, END
from langchain.agents import AgentExecutor, create_openai_functions_agent
from langchain_openai import ChatOpenAI
from .state import AgentState
from .tools import create_tools

def create_agent_graph() -> AgentExecutor:
    """创建旅行规划Agent"""
    
    # 创建LLM
    llm = ChatOpenAI(
        model="MiniMax/MiniMax-M2.5",
        temperature=0.7,
        base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
        api_key=os.getenv("ORCH_API_KEY")
    )
    
    # 创建Tools
    tools = create_tools()
    
    # 创建Agent
    prompt = ChatPromptTemplate.from_messages([
        ("system", TRAVEL_AGENT_PROMPT),
        ("user", "{input}"),
        ("assistant", "{agent_scratchpad}"),
    ])
    
    agent = create_openai_functions_agent(llm, tools, prompt)
    executor = AgentExecutor(agent=agent, tools=tools, max_iterations=5)
    
    return executor

# 全局Agent实例
agent = create_agent_graph()
```

---

## 四、Skills实现

### 4.1 Skill基类

```python
# src/skills/base.py
from abc import ABC, abstractmethod
from typing import TypedDict

class SkillInput(TypedDict):
    """Skill 输入"""
    query_params: dict
    context: dict

class SkillOutput(TypedDict):
    """Skill 输出"""
    success: bool
    data: dict | list | None
    error: str | None
    metadata: dict

class BaseSkill(ABC):
    """Skill 基类"""
    
    name: str = "base_skill"
    description: str = "基础技能"
    
    @abstractmethod
    async def execute(self, input_data: SkillInput) -> SkillOutput:
        """执行 Skill"""
        pass
    
    def can_handle(self, intent: dict) -> bool:
        """判断是否能处理该意图"""
        return intent.get("action") == self.name
    
    def _create_error_output(self, error: str) -> SkillOutput:
        """创建错误输出"""
        return {
            "success": False,
            "data": None,
            "error": error,
            "metadata": {"skill": self.name}
        }
```

### 4.2 天气查询Skill

```python
# src/skills/weather.py
from .base import BaseSkill, SkillInput, SkillOutput
from ..data_sources.weather import OpenMeteoAPI

class WeatherSkill(BaseSkill):
    """天气查询 Skill"""
    
    name = "weather"
    description = "查询目的地天气"
    
    def __init__(self):
        self.weather_api = OpenMeteoAPI()
    
    async def execute(self, input_data: SkillInput) -> SkillOutput:
        """执行天气查询"""
        try:
            params = input_data["query_params"]
            city = params.get("city")
            
            if not city:
                return self._create_error_output("缺少城市参数")
            
            # 调用天气API
            data = await self.weather_api.get_weather(city)
            
            return {
                "success": True,
                "data": data,
                "error": None,
                "metadata": {
                    "skill": self.name,
                    "city": city
                }
            }
            
        except Exception as e:
            return self._create_error_output(str(e))
```

### 4.3 车票查询Skill

```python
# src/skills/ticket.py
from .base import BaseSkill, SkillInput, SkillOutput
from ..data_sources.mcp_client import MCPClient

class TicketSkill(BaseSkill):
    """车票查询 Skill"""
    
    name = "ticket"
    description = "查询火车票信息"
    
    def __init__(self):
        self.mcp_client = MCPClient()
    
    async def execute(self, input_data: SkillInput) -> SkillOutput:
        """执行车票查询"""
        try:
            params = input_data["query_params"]
            date = params.get("date")
            from_station = params.get("from_station")
            to_station = params.get("to_station")
            
            if not all([date, from_station, to_station]):
                return self._create_error_output("缺少必要参数")
            
            # 调用12306 MCP
            data = await self.mcp_client.get_tickets(
                date=date,
                from_station=from_station,
                to_station=to_station,
                train_type=params.get("train_type", "G")
            )
            
            return {
                "success": True,
                "data": data,
                "error": None,
                "metadata": {
                    "skill": self.name,
                    "date": date,
                    "from": from_station,
                    "to": to_station
                }
            }
            
        except Exception as e:
            return self._create_error_output(str(e))
```

### 4.4 景点查询Skill

```python
# src/skills/attraction.py
from .base import BaseSkill, SkillInput, SkillOutput
from ..data_sources.baidu_search import BaiduSearchAPI

class AttractionSkill(BaseSkill):
    """景点查询 Skill"""
    
    name = "attraction"
    description = "搜索景点、美食、当地推荐"
    
    def __init__(self):
        self.baidu_search = BaiduSearchAPI()
    
    async def execute(self, input_data: SkillInput) -> SkillOutput:
        """执行景点查询"""
        try:
            params = input_data["query_params"]
            city = params.get("city")
            keyword = params.get("keyword", "景点")
            
            if not city:
                return self._create_error_output("缺少城市参数")
            
            # 调用百度搜索
            data = await self.baidu_search.search(city, keyword)
            
            return {
                "success": True,
                "data": data,
                "error": None,
                "metadata": {
                    "skill": self.name,
                    "city": city,
                    "keyword": keyword
                }
            }
            
        except Exception as e:
            return self._create_error_output(str(e))
```

---

## 五、数据源适配器

### 5.1 Open-Meteo天气API

```python
# src/data_sources/weather.py
"""Open-Meteo 天气API - 开源免费"""

import aiohttp
from typing import Optional
import logging

logger = logging.getLogger(__name__)

# 城市经纬度映射（常见城市）
CITY_COORDS = {
    "北京": {"lat": 39.90, "lon": 116.41},
    "上海": {"lat": 31.23, "lon": 121.47},
    "广州": {"lat": 23.13, "lon": 113.26},
    "深圳": {"lat": 22.54, "lon": 114.06},
    "成都": {"lat": 30.67, "lon": 104.07},
    "杭州": {"lat": 30.27, "lon": 120.15},
    "西安": {"lat": 34.34, "lon": 108.94},
    "重庆": {"lat": 29.56, "lon": 106.55},
    "南京": {"lat": 32.06, "lon": 118.79},
    "武汉": {"lat": 30.59, "lon": 114.31},
    # 可继续添加更多城市
}


class OpenMeteoAPI:
    """Open-Meteo 天气API 客户端"""
    
    BASE_URL = "https://api.open-meteo.com/v1/forecast"
    
    async def get_weather(self, city: str) -> dict:
        """获取城市天气"""
        
        # 查找城市坐标
        coords = CITY_COORDS.get(city)
        if not coords:
            # 尝试通过百度API获取坐标（降级方案）
            coords = await self._get_coords_from_baidu(city)
            if not coords:
                raise ValueError(f"不支持的城市: {city}")
        
        params = {
            "latitude": coords["lat"],
            "longitude": coords["lon"],
            "current_weather": "true",
            "hourly": "temperature_2m,precipitation_probability,weathercode",
            "daily": "temperature_2m_max,temperature_2m_min,precipitation_sum",
            "timezone": "Asia/Shanghai",
            "forecast_days": 7
        }
        
        async with aiohttp.ClientSession() as session:
            async with session.get(self.BASE_URL, params=params) as resp:
                if resp.status != 200:
                    raise Exception(f"API调用失败: {resp.status}")
                
                data = await resp.json()
                return self._parse_weather(data, city)
    
    def _parse_weather(self, data: dict, city: str) -> dict:
        """解析天气数据"""
        
        current = data.get("current_weather", {})
        daily = data.get("daily", {})
        
        # 天气代码映射
        weather_codes = {
            0: "晴",
            1: "晴间多云", 2: "多云", 3: "阴",
            45: "雾", 48: "霜雾",
            51: "小毛毛雨", 53: "中雨", 55: "大雨",
            61: "小雨", 63: "中雨", 65: "大雨",
            71: "小雪", 73: "中雪", 75: "大雪",
            80: "阵雨", 81: "阵雨", 82: "强阵雨",
            95: "雷暴", 96: "雷暴", 99: "雷暴"
        }
        
        result = {
            "城市": city,
            "当前天气": {
                "温度": f"{current.get('temperature', 'N/A')}°C",
                "天气": weather_codes.get(current.get('weathercode'), "未知"),
                "风速": f"{current.get('windspeed', 'N/A')} km/h",
                "风向": self._get_wind_direction(current.get('winddirection', 0))
            },
            "未来几天": []
        }
        
        # 解析未来几天预报
        dates = daily.get("time", [])
        max_temps = daily.get("temperature_2m_max", [])
        min_temps = daily.get("temperature_2m_min", [])
        precip = daily.get("precipitation_sum", [])
        
        for i, date in enumerate(dates):
            result["未来几天"].append({
                "日期": date,
                "最高温": f"{max_temps[i]}°C" if i < len(max_temps) else "N/A",
                "最低温": f"{min_temps[i]}°C" if i < len(min_temps) else "N/A",
                "降水量": f"{precip[i]}mm" if i < len(precip) else "N/A"
            })
        
        return result
    
    def _get_wind_direction(self, degrees: float) -> str:
        """风速方向转换"""
        directions = ["北", "东北", "东", "东南", "南", "西南", "西", "西北"]
        index = int((degrees + 22.5) // 45) % 8
        return directions[index]
    
    async def _get_coords_from_baidu(self, city: str) -> Optional[dict]:
        """从百度API获取城市坐标（降级方案）"""
        # TODO: 实现百度地图API调用
        return None
```

### 5.2 12306 MCP客户端

```python
# src/data_sources/mcp_client.py
"""12306 MCP 客户端 - 整合现有MCP服务"""

import requests
import json
from typing import Optional
import logging

logger = logging.getLogger(__name__)


class MCPClient:
    """12306 MCP 客户端"""
    
    def __init__(self, base_url: str = "http://localhost:8000"):
        self.base_url = base_url
    
    def get_current_date(self) -> str:
        """获取当前日期"""
        try:
            resp = requests.post(
                f"{self.base_url}/tools/get-current-date",
                json={}
            )
            return resp.json().get("content", "")
        except Exception as e:
            logger.error(f"获取当前日期失败: {e}")
            from datetime import datetime
            return datetime.now().strftime("%Y-%m-%d")
    
    def get_station_code(self, city: str) -> Optional[dict]:
        """获取城市车站代码"""
        try:
            resp = requests.post(
                f"{self.base_url}/tools/get-station-code-of-citys",
                json={"city": city}
            )
            return resp.json()
        except Exception as e:
            logger.error(f"获取车站代码失败: {e}")
            return None
    
    def get_tickets(self, date: str, from_station: str, 
                   to_station: str, train_type: str = "G") -> dict:
        """查询火车票"""
        
        # 1. 获取车站代码
        from_codes = self.get_station_code(from_station)
        to_codes = self.get_station_code(to_station)
        
        if not from_codes or not to_codes:
            raise ValueError(f"无法找到车站: {from_station} 或 {to_station}")
        
        # 取第一个车站
        from_code = list(from_codes.values())[0]["station_code"]
        to_code = list(to_codes.values())[0]["station_code"]
        
        # 2. 查询余票
        try:
            resp = requests.post(
                f"{self.base_url}/tools/get-tickets",
                json={
                    "date": date,
                    "fromStation": from_code,
                    "toStation": to_code,
                    "trainFilterFlags": train_type
                }
            )
            return resp.json()
        except Exception as e:
            logger.error(f"查询余票失败: {e}")
            return {"error": str(e)}
```

### 5.3 百度搜索降级

```python
# src/data_sources/baidu_search.py
"""百度搜索API - Fallback方案"""

import aiohttp
import os
import json
import logging

logger = logging.getLogger(__name__)


class BaiduSearchAPI:
    """百度搜索API 客户端"""
    
    BASE_URL = "https://qianfan.baidubce.com/v2/ai_search/web_search"
    
    def __init__(self):
        self.api_key = os.getenv("BAIDU_SEARCH_API_KEY", 
            "bce-v3/ALTAK-W82EktzpEt35uOmI3Kzko/527009dbca41ceae303e11eda074a516222a593f")
    
    async def search(self, city: str, keyword: str = "景点") -> dict:
        """执行搜索"""
        
        query = f"{city} {keyword}"
        
        headers = {
            "Authorization": f"Bearer {self.api_key}",
            "Content-Type": "application/json"
        }
        
        payload = {
            "query": query,
            "max_results": 10
        }
        
        try:
            async with aiohttp.ClientSession() as session:
                async with session.post(
                    self.BASE_URL, 
                    json=payload, 
                    headers=headers
                ) as resp:
                    if resp.status != 200:
                        logger.warning(f"百度搜索失败: {resp.status}")
                        return await self._fallback_search(query)
                    
                    data = await resp.json()
                    return self._parse_search_results(data)
        
        except Exception as e:
            logger.error(f"百度搜索异常: {e}")
            return await self._fallback_search(query)
    
    async def _fallback_search(self, query: str) -> dict:
        """降级方案：使用通用搜索"""
        # TODO: 实现降级搜索逻辑
        return {
            "query": query,
            "results": [],
            "error": "搜索服务暂时不可用"
        }
    
    def _parse_search_results(self, data: dict) -> dict:
        """解析搜索结果"""
        
        results = data.get("results", [])
        parsed = []
        
        for item in results:
            parsed.append({
                "标题": item.get("title", ""),
                "摘要": item.get("snippet", ""),
                "链接": item.get("url", "")
            })
        
        return {
            "results": parsed,
            "count": len(parsed)
        }
```

---

## 六、LLM客户端与Prompt

### 6.1 MiniMax客户端

```python
# src/llm/client.py
import os
import json
from langchain_openai import ChatOpenAI
from langchain_core.messages import HumanMessage, SystemMessage
from .prompts import TRAVEL_AGENT_PROMPT


class MiniMaxClient:
    """MiniMax 模型客户端"""
    
    def __init__(self):
        self.client = ChatOpenAI(
            model=os.getenv("ORCH_MODEL", "MiniMax/MiniMax-M2.5"),
            temperature=0.7,
            max_tokens=2000,
            base_url=os.getenv("ORCH_API_BASE"),
            api_key=os.getenv("ORCH_API_KEY")
        )
    
    def chat(self, messages: list, **kwargs) -> str:
        """发送聊天请求"""
        
        response = self.client.invoke(messages)
        return response.content
    
    def chat_with_tools(self, user_input: str, tools: list) -> dict:
        """使用工具调用"""
        # LangChain Agent 自动处理
        pass
```

### 6.2 Prompt模板

```python
# src/llm/prompts.py

TRAVEL_AGENT_PROMPT = """你是一个专业的旅行规划助手，帮助用户规划国内外的旅行行程。

## 你的能力
1. 查询目的地天气 - 使用 get_weather 工具
2. 查询火车票 - 使用 get_train_tickets 工具
3. 搜索景点美食 - 使用 search_attractions 工具
4. 获取旅行注意事项 - 使用 get_travel_tips 工具
5. 当以上工具无法满足时，使用百度搜索辅助

## 工作流程
1. 理解用户需求（目的地、时间、预算、偏好）
2. 按需调用工具获取信息
3. 整合所有信息，给出完整的行程建议

## 输出格式要求
- 行程规划要具体，包含每日安排
- 列出推荐的景点、美食、交通方式
- 提醒当地天气和注意事项
- 语言要友好自然，符合中文表达习惯

## 注意事项
- 如果用户没有提供具体日期，询问用户出发日期
- 如果工具调用失败，尝试使用百度搜索作为降级方案
- 不要假设任何信息，必须通过工具获取
- 火车票查询需要具体的出发日期

现在，请帮助用户规划他们的旅行！"""
```

---

## 七、Web前端设计

### 7.1 技术选型

| 技术 | 说明 |
|------|------|
| Vue 3 | 渐进式前端框架 |
| Vite | 构建工具 |
| SSE | 服务端推送流式响应 |

### 7.2 界面设计

```vue
<!-- frontend/src/App.vue -->
<template>
  <div class="app-container">
    <header class="header">
      <h1>✈️ 旅行规划助手</h1>
      <p>智能规划您的每一次旅行</p>
    </header>
    
    <main class="chat-container">
      <div class="messages" ref="messagesRef">
        <div 
          v-for="(msg, index) in messages" 
          :key="index"
          :class="['message', msg.role]"
        >
          <div class="avatar">
            {{ msg.role === 'user' ? '👤' : '✈️' }}
          </div>
          <div class="content">
            <div v-if="msg.role === 'assistant' && msg.isStreaming" class="typing-indicator">
              <span></span><span></span><span></span>
            </div>
            <div v-html="formatMessage(msg.content)"></div>
          </div>
        </div>
      </div>
      
      <div class="input-area">
        <input 
          v-model="userInput" 
          @keyup.enter="sendMessage"
          placeholder="请输入您的旅行需求，如：帮我规划一个去上海的三天两夜旅行"
          :disabled="isLoading"
        />
        <button @click="sendMessage" :disabled="isLoading">
          {{ isLoading ? '发送中...' : '发送' }}
        </button>
      </div>
    </main>
  </div>
</template>

<script setup>
import { ref, onMounted } from 'vue'

const messages = ref([
  {
    role: 'assistant',
    content: '您好！我是您的旅行规划助手 ✈️\n\n我可以帮您：\n- 查询目的地天气\n- 查询火车票/机票\n- 推荐景点和美食\n- 制定完整行程规划\n\n请告诉我您想去哪里旅行？'
  }
])
const userInput = ref('')
const isLoading = ref(false)
const messagesRef = ref(null)

const sendMessage = async () => {
  if (!userInput.value.trim() || isLoading.value) return
  
  const userMsg = { role: 'user', content: userInput.value }
  messages.value.push(userMsg)
  userInput.value = ''
  isLoading.value = true
  
  try {
    const response = await fetch('http://localhost:8000/chat', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ message: userMsg.content })
    })
    
    const reader = response.body.getReader()
    const decoder = new TextDecoder()
    
    messages.value.push({ role: 'assistant', content: '', isStreaming: true })
    
    while (true) {
      const { done, value } = await reader.read()
      if (done) break
      messages.value[messages.value.length - 1].content += decoder.decode(value)
    }
    
    messages.value[messages.value.length - 1].isStreaming = false
  } catch (error) {
    messages.value.push({
      role: 'assistant',
      content: '抱歉，发生了错误，请稍后重试。'
    })
  }
  
  isLoading.value = false
}

const formatMessage = (content) => {
  return content.replace(/\n/g, '<br>')
}
</script>

<style>
/* 样式略，可参考 anime-agent 前端样式 */
</style>
```

---

## 八、环境配置

### 8.1 依赖安装

```bash
# 创建虚拟环境
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Linux/Mac

# 安装依赖
pip install -r requirements.txt
```

### 8.2 requirements.txt

```
langchain>=0.1.0
langchain-openai>=0.0.5
langgraph>=0.0.20
langchain-core>=0.1.10
aiohttp>=3.9.0
python-dotenv>=1.0.0
loguru>=0.7.0
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
requests>=2.31.0
```

### 8.3 环境变量配置

```env
# MiniMax 配置（必需）
ORCH_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
ORCH_MODEL=MiniMax/MiniMax-M2.5
ORCH_API_KEY=sk-ac33d2e11d4a4080a61845e6bfc8a0e4

# 百度搜索 API（降级方案）
BAIDU_SEARCH_API_KEY=bce-v3/ALTAK-W82EktzpEt35uOmI3Kzko/527009dbca41ceae303e11eda074a516222a593f

# 12306 MCP 服务地址
MCP_BASE_URL=http://localhost:8000

# 日志配置
LOG_LEVEL=INFO

# 缓存配置
CACHE_TTL=3600
```

---

## 九、快速启动

### 9.1 启动12306 MCP服务

```bash
# 进入MCP目录
cd mcps/12306-mcp

# 启动MCP服务（参考MCP项目文档）
# 确保服务运行在 http://localhost:8000
```

### 9.2 启动后端服务

```bash
# 进入项目目录
cd travel-agent

# 激活虚拟环境
venv\Scripts\activate

# 启动FastAPI服务
uvicorn src.main:app --reload

# 访问 API 文档
# http://localhost:8000/docs
```

### 9.3 启动前端

```bash
# 进入前端目录
cd frontend

# 安装前端依赖
npm install

# 启动开发服务器
npm run dev

# 访问聊天界面
# http://localhost:5173
```

---

## 十、测试用例

### 10.1 功能测试

| 测试项 | 输入 | 预期输出 |
|--------|------|----------|
| 天气查询 | "北京后天天气怎么样" | 返回北京天气预报 |
| 车票查询 | "查一下明天北京到上海的高铁" | 返回车次列表 |
| 景点推荐 | "上海有什么好玩的地方" | 返回景点列表 |
| 行程规划 | "帮我规划去杭州三天两夜" | 返回完整行程 |

### 10.2 异常场景

| 测试项 | 输入 | 预期行为 |
|--------|------|----------|
| 缺少日期 | "帮我查北京到上海的火车票" | 询问具体日期 |
| API失败 | 12306服务不可用 | 降级到百度搜索 |
| 未知城市 | "查一下亚特兰大的天气" | 返回不支持提示 |

---

## 十一、后续迭代计划

### v1.1.0
- [ ] 支持机票查询（接入航班API）
- [ ] 添加行程预览图

### v1.2.0
- [ ] 支持多语言
- [ ] 添加用户偏好记忆

### v2.0.0
- [ ] 支持酒店预订
- [ ] 支持行程分享

---

*文档版本: 1.0.0*
*创建日期: 2026-03-12*
